"""Stub module for the `provenex` PyPI meta-package.

This module exists only so the meta-package has something to build into a
wheel. It is never meant to be imported — `import provenex` should resolve
to the real package installed by `provenex-core` (a runtime dependency).

If you find yourself reading this code, you probably want
``provenex.core`` or ``provenex.index`` from the `provenex-core`
distribution. See https://github.com/provenex/provenex-core.
"""

__all__ = []
